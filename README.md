# burying the bug

A full-stack application.

## Prerequisites

- Node.js

## Setup

1. Install dependencies across backend, frontend, and root:
```bash
npm install
cd backend && npm install
cd ../frontend && npm install
```

2. Start the development servers:
```bash
npm start
```
